# Set automatically by Vercel once you attach a Postgres database to this
# project (Storage tab -> Create Database -> Postgres, powered by Neon).
# For local development, copy this file to .env.local and fill in a real
# connection string from your Neon/Vercel Postgres dashboard.
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
